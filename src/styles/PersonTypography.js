import { Typography } from "@mui/material";
import styled from "@emotion/styled";
import { borderRadius } from "@mui/system";


export const PersonTypography = styled(Typography)(()=>({
  color:'white',
  fontWeight:'400',
  margin:'8px 0',

}))